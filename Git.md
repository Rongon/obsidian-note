Git

# 常用 git 指令

## 基础：起步与提代码

- **拉取远程代码：**

```bash
git clone <仓库地址>
```

- **查看当前分支状态：**（看哪些文件改了、哪些没提交，极常用）

```bash
git status
```

- 暂存修改：

```bash
git add .          # 暂存所有改动
git add <文件名>    # 仅暂存特定文件
```

- 提交保存：

```bash
git commit -m "feat: 新增用户登录功能"
```

**求职加分项：** 规范的 commit message（如使用 `feat:`, `fix:`, `docs:` 等前缀）会显得你非常有团队协作经验。

---

## 进阶：多分支协作

前端开发通常在自己独立的分支开发，最后合并到主分支：

- **查看分支：**

```bash
git branch
```

- 创建并切换到新分支：

```bash
git checkout -b <新分支名>   # 旧版命令
git switch -c <新分支名>     # 新版推荐命令
```

- 切换分支：

```bash
git checkout <分支名>
git switch <分支名>
```

- **拉取最新代码并合并：**

```bash
git pull origin <分支名>
```

- 推送本地代码到远程：

```bash
git push origin <分支名>
```

- **合并分支：**（将 feature 分支合并到 main/dev）

```bash
git merge <分支名>
```

---

- **场景一：代码写了一半，需要紧急切换到其他分支修 Bug**

  - 使用 **`git stash`**（暂存当前未提交的改动）：

  ```bash
  git stash        # 封存当前修改，让工作区变干净
  # 切换去修 Bug ... 修完回来：
  git stash pop    # 还原之前写了一半的代码
  ```

- **场景二：刚刚 commit 的信息写错了，或者漏提交了文件**

  - 使用 **`--amend`**（微调上一次提交）：

  ```bash
  git commit --amend -m "正确的提交信息"
  ```

- **场景三：代码彻底写烂了，想回退到上一个版本**

  - 使用 **`git reset`**：

  ```bash
  git reset --hard HEAD^     # 强行回退到上一个版本（慎用，会丢弃未提交的修改）
  git reset --soft HEAD^     # 回退上一次 commit，但保留代码改动在暂存区
  ```

---

## 常用的commit前缀

**`feat`** (feature)：**新增功能**

- **何时用**：只要你为项目写了新页面、新组件、新接口或新交互逻辑。
- **示例**：`feat(login): 新增验证码登录功能`

**`fix`** (bug fix)：**修复 Bug**

- **何时用**：修复了任何代码缺陷、样式错乱、线上报错或逻辑漏洞。
- **示例**：`fix(cart): 修复多次点击结算导致重复提交的问题`

**`docs`** (documentation)：**文档相关**

- **何时用**：修改了项目的 `README.md`、开发文档、API 接口说明，或者代码中的**注释**。
- **示例**：`docs: 在 README 中补充本地启动和打包命令`

**`style`**：**代码格式调整（不影响代码逻辑）**

- **何时用**：不改变代码运行逻辑，仅仅调整了空格、缩进、分号、格式化代码、删除无用 console 等。
- **示例**：`style: 统一全线组件的缩进为 2 空格`

**`refactor`**：**重构（既不是新功能，也不是修 Bug）**

- **何时用**：优化了代码结构、提取了公共组件、重命名了变量、优化了条件判断，但功能表现和以前完全一致。
- **示例**：`refactor: 提取公共列表组件，减少冗余代码`

**`perf`** (performance)：**性能优化**

- **何时用**：专门为了提升网页加载速度、运行流畅度或减少打包体积而做的改动。
- **示例**：`perf: 路由懒加载，优化首屏加载速度`

**`chore`** (杂务)：**构建工具或辅助工具变动**

- **何时用**：不修改业务代码，只是更新了 `package.json` 依赖、修改了 `vite.config.js`/`webpack.config.js` 配置文件、或者调整了 `.gitignore`。
- **示例**：`chore: 升级 axios 到 v1.x 版本`

**`test`**：**测试相关**

- **何时用**：编写或修改单元测试（Jest/Vitest）、集成测试、端到端测试。
- **示例**：`test: 补充购物车组件的单元测试用例`

---

## 旧checkout 和新 switch

| **职责（干什么事）**    | **传统命令（老版）**               | **拆分后的现代命令（推荐）**           |
| ----------------------- | ---------------------------------- | -------------------------------------- |
| **切换分支 / 新建分支** | `git checkout` / `git checkout -b` | **`git switch`** / **`git switch -c`** |
| **撤销修改 / 恢复文件** | `git checkout -- <file>`           | **`git restore`**                      |

---

