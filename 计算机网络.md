# OSI七层模型
## 物理层

![图片描述](https://raw.githubusercontent.com/Rongon/obsidian-note/main/20260730174452600.png)

在这一层通过（电，光，无线电波），会获取他们对应的传送信号、电压，转换成010101010101但是数据还未组织，它们的单位为bit，所以在这一层主要就是传输比特流

---
## 数据链路层

MAC地址、数据帧

---
## 网络层

ip、数据包、路由器、

---

## 传输层

端口号、TCP/UDP、数据段、

---
## 会话层

会话层，是在发送方和接收方之间进行通信时创建、维持、之后终止或断开连接的地方，与电话通话有点相似。

报文、

---
## 表示层

表示层主要做了几件重要的事情安全，压缩，也是程序在网络中的一个翻译官。

报文、

---
## 应用层

例如ajax调用接口发送http请求，
再比如域名系统DNS，
邮件协议SMTP，
webSocket长连接，
SSH协议

报文、

---

# TCP（全双工）
## 三次握手

状态：
1. seq(sequence number)，序列号随机生成的
2. ack (acknowledgement number) 确认号 ack = seq + 1
3. ACK (acknowledgement)确定序列号有效
4. SYN(synchronous）发起新连接
---

![图片描述](https://raw.githubusercontent.com/Rongon/obsidian-note/main/20260730191542174.png)

---

假设：seq序列号随机生成

- **客户端初始序列号 (ISN_C) = 100**
  
- **服务端初始序列号 (ISN_S) = 300**
  

### 第一次握手：客户端 → 服务端

客户端说：“服务端，我想和你同步，我的初始序列号是 100。”

| 字段      | 值     | 含义                    |
| ------- | ----- | --------------------- |
| **SYN** | `1`   | 表示这是一个同步请求            |
| **ACK** | `0`   | 第一个包，还没有确认号           |
| **seq** | `100` | 客户端告诉服务端：我的编号从 100 开始 |
| **ack** | `0`   | 无意义（因为 ACK 标志为 0）     |
**此时客户端状态**：`SYN-SENT`（同步已发送）

---

### 第二次握手：服务端 → 客户端

服务端收到后，回复两条信息在同一个包里：“我收到了，你的 100 我记下了；同时，我的初始序列号是 300。”

| 字段      | 值     | 含义                                             |
| ------- | ----- | ---------------------------------------------- |
| **SYN** | `1`   | 服务端也发起自己的同步                                    |
| **ACK** | `1`   | 确认号字段生效（从这次开始，后续报文该位恒为 1）                      |
| **seq** | `300` | 服务端告诉客户端：我的编号从 300 开始                          |
| **ack** | `101` | **= 客户端 seq + 1**，表示“你的 100 我收到了，下次请从 101 开始发” |

**此时服务端状态**：`SYN-RCVD`（同步已收到）

---

### 第三次握手：客户端 → 服务端

客户端收到后，回复：“我也收到你的 300 了，连接正式建立。”

| 字段      | 值     | 含义                                             |
| ------- | ----- | ---------------------------------------------- |
| **SYN** | `0`   | 同步已完成，不再是同步请求                                  |
| **ACK** | `1`   | 确认号字段有效                                        |
| **seq** | `101` | = **服务端发来的 ack**，因为客户端要按服务端的期望发送               |
| **ack** | `301` | **= 服务端 seq + 1**，表示“你的 300 我收到了，下次请从 301 开始发” |

**双方状态**：都进入 `ESTABLISHED`（连接已建立）

---

## 四次挥手

四次挥手的核心，就是**每一方都需要单独发送一个FIN包来关闭自己这一侧的数据发送，并由对方回复ACK来确认**

状态：
1. seq(sequence number)，序列号随机生成的
2. ack (acknowledgement number)确认号 ack = seq +1
3. ACK (acknowledgement）确定序列号有效
4. SYN(synchronous）发起新连接

---

![图片描述](https://raw.githubusercontent.com/Rongon/obsidian-note/main/20260730194301426.png)

---
我们假设在正式挥手前：  
	客户端向服务端发送了 **10 字节**数据（seq=101~110），服务端向客户端发送了 **20 字节**数据（seq=301~320）。
因此，挥手开始前的基准状态是：  
	**客户端**：下次发送序号 `seq = 111`，期望收到的确认号 `ack = 321`（表示已收到服务端的 320 号字节）。  
	**服务端**：下次发送序号 `seq = 321`，期望收到的确认号 `ack = 111`（表示已收到客户端的 110 号字节）。

FIN:1
ACK:1
seq:111
ack:321

FIN:0
ACK:1
seq:321
ack:112

FIN:1
ACK:1
seq:

### ### 第一次挥手（客户端 → 服务端）

客户端发起关闭请求。

|字段|值|含义|
|---|---|---|
|**FIN**|`1`|请求关闭客户端→服务端方向的连接|
|**ACK**|`1`|确认号有效（连接建立后，该位恒为 1）|
|**seq**|`111`|客户端的当前发送序号（接着上次数据的末尾）|
|**ack**|`321`|告诉服务端：“你的 320 号字节我全收到了，下次请从 321 开始发”|

> **客户端状态**：`FIN_WAIT_1`（等待服务端确认我的 FIN）

---
### 第二次挥手（服务端 → 客户端）

服务端立刻回应确认。

|字段|值|含义|
|---|---|---|
|**FIN**|`0`|纯确认包，不携带关闭请求|
|**ACK**|`1`|确认号有效|
|**seq**|`321`|服务端当前序号（纯 ACK 包不消耗序号，所以不变）|
|**ack**|`112`|**= 客户端 seq(111) + 1**，表示：“你的 FIN 序号 111 我收到了，下次请从 112 开始”|

> **服务端状态**：`CLOSE_WAIT`（等待自己应用层处理完剩余数据）  
> **客户端状态**：`FIN_WAIT_2`（收到确认，等待服务端发 FIN）
---
### ### 第三次挥手（服务端 → 客户端）

服务端数据处理完毕，正式发起关闭。

|字段|值|含义|
|---|---|---|
|**FIN**|`1`|请求关闭服务端→客户端方向的连接|
|**ACK**|`1`|确认号有效|
|**seq**|`321`|注意：因为第二次挥手是纯 ACK（不消耗序号），且这期间没发数据，所以序号仍然是 321|
|**ack**|`112`|**= 客户端 seq(111) + 1**，同上，继续确认客户端的 FIN|

> **服务端状态**：`LAST_ACK`（等待客户端对这条 FIN 的最终确认）

---
### 第四次挥手（客户端 → 服务端）

客户端收到 FIN 后，发送最终确认。

|字段|值|含义|
|---|---|---|
|**FIN**|`0`|纯确认包|
|**ACK**|`1`|确认号有效|
|**seq**|`112`|客户端的当前序号（纯 ACK 包不消耗序号，延用第二次挥手中的 ack 值）|
|**ack**|`322`|**= 服务端 seq(321) + 1**，表示：“你的 FIN 序号 321 我收到了，下次请从 322 开始”|

> **客户端状态**：`TIME_WAIT`（等待 2MSL 后关闭）  
> **服务端状态**：收到此包后立即进入 `CLOSED`

**`TIME_WAIT` 的 2MSL 本质上是一个“静默观察期”。如果观察期内风平浪静（没收到重传 FIN），则证明对手（服务端）已确认收兵；此时客户端默默撤退，双方完美“和平分手”。**

---
123123123