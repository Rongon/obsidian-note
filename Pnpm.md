pnpm

# 两个问题

## 👻 幽灵依赖（Phantom Dependencies）

**1. 什么是幽灵依赖？**
幽灵依赖是指，你的项目代码里 `import` 了一个包（比如 `lodash`），但你的 `package.json` 里并没有声明它。因为它是被你安装的其他包（比如某个组件库）顺带安装进来的。

**2. 为什么 `npm` 会有这个问题？**
`npm` 传统的 `node_modules` 是扁平化的结构。它会尽可能把所有依赖都提升到顶层，这就导致所有包都能互相访问，你安装的包 A 可以偷偷访问你安装的包 B，你也可以访问到被偷偷带进来的包。

**3. `pnpm` 是如何解决的？**
`pnpm` 使用**非扁平化的 `node_modules`**。它会给每个包创建独立的文件夹，通过**符号链接**把它们串联起来。你的项目只能访问在 `package.json` 中声明过的直接依赖。如果你尝试 `import` 一个没安装过的幽灵包，`pnpm` 会在模块解析层直接报错，从根本上杜绝了这个问题。

---

“幽灵依赖”会导致什么问题？

**项目运行的稳定性差**，以及**依赖升级和重构的风险高**。

 1. 项目运行的不确定性

幽灵依赖就像是埋在项目里的定时炸弹。你今天能用 `import lodash from 'lodash'`，是因为你的某个依赖（比如 `some-lib`）恰好把它带了进来。

- **随时可能失效**：一旦 `some-lib` 在更新后不再依赖 `lodash`，你的项目就会突然报错，因为 `lodash` 已经不在你的 `node_modules` 里了。
- **CI/CD 环境和线上构建失败**：你可能本地能跑，但服务器构建时，依赖的安装顺序或版本不同，导致幽灵依赖没被装进来，构建就会直接失败，这种问题排查起来非常痛苦。

 2. 项目维护的混乱

- **依赖关系不清晰**：对于接手项目的开发者来说，他看到你在用 `lodash`，但 `package.json` 里却没有声明。他会无法判断这是一个被遗漏的依赖，还是一个即将被移除的旧代码。这给代码审查和项目交接带来了巨大的认知负担。
- **升级和重构的障碍**：当你升级某个主依赖时，它的子依赖也会跟着变。你根本不知道这次升级会不会悄悄改变或移除你正依赖的那个幽灵包，这让本应很常规的升级操作变得风险极高。

---

## 💾 节省磁盘空间

**1. 为什么能节省？**
传统的包管理器，你每新建一个项目，哪怕用的是相同的依赖，都要把文件重新拷贝一份，很占空间。

**2. `pnpm` 是怎么做的？**
`pnpm` 使用**基于内容的寻址**。它会把你电脑上所有下载过的依赖，都存在一个全局仓库里（`~/.pnpm-store`）。当你再往项目里安装依赖时，`pnpm` 并不会复制文件，而是创建**硬链接**，直接指向全局仓库里的那唯一一份文件。这样，100个项目用的 `react`，在磁盘上只有1份文件，自然就省了大量空间。

---

# 指令

- **装所有依赖**：`pnpm install`
- **安装单个包**：`pnpm add <package>`
- **开发依赖**：`pnpm add -D <package>`
- **全局安装**：`pnpm add -g <package>`
- **卸载包**：`pnpm remove <package>`
- **运行脚本**：`pnpm run <script>`

---

- **`pnpm dlx`（代替 `npx`）**：用于临时下载并运行某个包。比如 `pnpm dlx create-react-app my-app`。面试时可能会问到，它相比 `npx` 的优势是更严格，不会和已安装的全局依赖混在一起。
- **`-w` 和 `--filter`（Monorepo 常用）**：这是 pnpm 在处理大型项目时的核心优势。
  - `-w`：表示在 monorepo 的根目录操作，而不是某个子包。
  - `--filter`：允许你精确地选择对哪个子包执行命令，比如 `pnpm --filter my-app add lodash`。
    如果你不面大厂，这两点可以暂时只做了解，但能说出来绝对是加分项。
- **`pnpm why <package>`（依赖分析）**：帮助你追溯为什么要安装某个包，用来排查幽灵依赖非常方便。

---

- **`pnpm store prune`**：清理全局存储中不再被任何项目引用的包，帮你节省宝贵的磁盘空间。
- **`pnpm audit`**：像 `npm audit` 一样，但速度快很多，可以检查项目中的已知安全漏洞。
- **`pnpm patch`**：这是 pnpm 的一个强大特性，允许你对 `node_modules` 中已安装的某个依赖文件，应用临时的补丁修复，而不需要等待上游包发布新版本。

---

