# js

## 经典面试题
### **基础语法：**

#### var、let、const 有什么区别？（8点）
- 块级作用域：块作用域由 `{ }`包括，let和const具有块级作用域，var不存在块级作用域。
- 变量提升：var存在变量提升，let和const不存在变量提升，即在变量只能在声明之后使用，否在会报错。
- 全局属性：浏览器的全局对象是window，Node的全局对象是global。var声明的变量为全局变量，并且会将该变量添加为全局对象的属性，但是let和const不会。
- 重复声明：var声明变量时，可以重复声明变量，后声明的同名变量会覆盖之前声明的遍历。const和let不允许重复声明变量。
- 暂时性死区：在使用let、const命令声明变量之前，该变量都是不可用的。
- 初始值设置：在变量声明时，var 和 let 可以不用设置初始值。而const声明变量必须设置初始值。
- 重新赋值：let和const都是ES6新增的用于创建变量的语法。 let创建的变量是可以更改指针指向（可以重新赋值）。但const声明的变量是不允许改变指针的指向。
---
#### JavaScript 有哪些数据类型？如何判断数据类型？
	JavaScript共有八种数据类型，分别是 Undefined、Null、Boolean、Number、String、Object、Symbol、BigInt。

1. typeof（判断原始类型最常用）
```js
console.log(typeof 2);               // number
console.log(typeof true);            // boolean
console.log(typeof 'str');           // string
console.log(typeof []);              // object    
console.log(typeof function(){});    // function
console.log(typeof {});              // object
console.log(typeof undefined);       // undefined
console.log(typeof null);            // object
```
其中数组、对象、null都会被判断为object，其他判断都正确。

2. instanceof（判断引用类型，检测原型链）
`instanceof`可以正确判断对象的类型，其内部运行机制是判断在其原型链中能否找到该类型的原型。
```js
console.log(2 instanceof Number);                    // false
console.log(true instanceof Boolean);                // false 
console.log('str' instanceof String);                // false 
 
console.log([] instanceof Array);                    // true
console.log(function(){} instanceof Function);       // true
console.log({} instanceof Object);                   // true
```
可以看到，`instanceof`只能正确判断引用数据类型，而不能判断基本数据类型。`instanceof` 运算符可以用来测试一个对象在其原型链中是否存在一个构造函数的 `prototype` 属性。

3. Object.prototype.toString.call()（万能方法，最准确）
`Object.prototype.toString.call()` 使用 Object 对象的原型方法 toString 来判断数据类型：
```js
var a = Object.prototype.toString;
 
console.log(a.call(2));
console.log(a.call(true));
console.log(a.call('str'));
console.log(a.call([]));
console.log(a.call(function(){}));
console.log(a.call({}));
console.log(a.call(undefined));
console.log(a.call(null));
```

4. `Array.isArray()`（专门判断数组）
```js
Array.isArray([])  // true
Array.isArray({})  // false
```
---
#### == 和 === 有什么区别？
	如果两边类型不一致，使用双等号判断时，则会进行强制类型转化后再进行比较；使用三等号判断时，不会做强制类型准换，直接返回 false。
---
#### null 和 undefined 有什么区别？（6点）

| 特性               | `undefined`                    | `null`                         |
| ---------------- | ------------------------------ | ------------------------------ |
| **语义**           | 变量已声明但**尚未赋值**                 | 变量已声明，但**故意设置**为空              |
| **产生场景**         | JS 引擎**自动**赋予                  | 开发者**手动**赋值                    |
| **`typeof` 结果**  | `'undefined'`                  | `'object'` (历史遗留 bug)          |
| **转换为数字**        | `NaN`                          | `0`                            |
| **`==` (宽松相等)**  | `undefined == null` 为 `true`   | `null == undefined` 为 `true`   |
| **`===` (严格相等)** | `undefined === null` 为 `false` | `null === undefined` 为 `false` |

---
### **函数和作用域**：

#### 什么是闭包？闭包有什么作用？

概念：闭包是指一个内部函数可以访问其外部函数中声明的变量和参数，即使在外部函数已经执行完毕之后。

---
例子：
```js
function outer() {
  let count = 0; // 这是被闭包“捕获”的变量

  return function inner() {
    count++;
    console.log(count);
  };
}

const fn = outer();
fn(); // 1
fn(); // 2
```

```vue
<!-- 在组件中使用 -->
<script setup>
// useCounter.js —— 组合式函数
import { ref } from 'vue'

export function useCounter() {
  const count = ref(0)  // count 被闭包“保护”起来

  function increment() { count.value++ }
  function decrement() { count.value-- }

  return { count, increment, decrement }
}

const { count, increment } = useCounter()
// count 的值只能通过暴露的 increment/decrement 修改
// 外部无法直接篡改 count 的内部逻辑
</script>
```

```jsx
// useCounter.js —— 自定义 Hook
import { useState } from 'react'

export function useCounter() {
  const [count, setCount] = useState(0)

  function increment() { setCount(c => c + 1) }
  function decrement() { setCount(c => c - 1) }

  return { count, increment, decrement }
}

// 在组件中使用
function Counter() {
  const { count, increment, decrement } = useCounter()

  return (
    <div>
      <span>{count}</span>
      <button onClick={increment}>+</button>
      <button onClick={decrement}>-</button>
    </div>
  )
}
```

---
作用：
- **保护数据私有性，避免全局变量污染**：这是闭包最核心的能力。它可以创建私有的、外部无法直接访问的变量，只能通过暴露的特定函数来修改。
- **解决循环中的 `var` 历史问题**：在 `let` 出现前，用 `var` 在循环中创建函数会有经典的变量共享问题，闭包是当时的主流解决方案。
- **实现高阶函数和模块化**：JavaScript 的模块模式（Module Pattern）就是利用闭包来封装私有变量和公共 API。这也是现代前端框架中，组合式 API、自定义 Hook 等概念的基石。
- **函数柯里化**①：通过闭包来保存部分参数，返回需要剩余参数的新函数。
---
副作用：需要注意的是，被闭包引用的变量不会被垃圾回收，滥用可能导致**内存泄漏**。

**什么时候必须手动回收**：当**你无法通过“自然解除引用”来让闭包被回收**时，就需要手动干预。

典型场景：
```js
// 解除变量引用
let fn = outer()
fn = null  // 切断引用，闭包可被回收

// 清除定时器
let id = setInterval(() => {
  console.log('running')
}, 1000)

// 不需要时清除
clearInterval(id)

// 移除事件监听器
function handler() { console.log('clicked') }
element.addEventListener('click', handler)

// 不需要时移除
element.removeEventListener('click', handler)

```
---
#### 箭头函数和普通函数的区别（7点）

总结对比表

| 特性              | 普通函数                     | 箭头函数                   |
| --------------- | ------------------------ | ---------------------- |
| **`this`**      | 动态绑定（调用时确定）              | 静态绑定（定义时继承外层作用域）       |
| **`arguments`** | 有                        | 无（用剩余参数 `...args` 替代）  |
| **构造函数**        | 可以 `new`                 | 不可以 `new`              |
| **`prototype`** | 有                        | 无                      |
| **语法**          | 需完整写 `{}` 和 `return`     | 简洁，可省略 `{}` 和 `return` |
| **改变 `this`**   | `call`/`apply`/`bind` 有效 | 无效                     |
| **生成器**         | 可以                       | 不可以                    |

---
1. `this` 绑定
这是**最重要**的区别，也是面试核心考点。

| 普通函数          | 箭头函数                    |                                |
| ------------- | ----------------------- | ------------------------------ |
| **`this` 指向**   | **动态**，取决于调用方式（谁调用就指向谁） | **静态**，继承自**定义时**外层作用域的 `this` |

---
2. **call()、apply()、bind()不能改变箭头函数的this指向**
```js
var id = 'Global';
let fun1 = () => {
    console.log(this.id)
};
fun1();                     // 'Global'
fun1.call({id: 'Obj'});     // 'Global'
fun1.apply({id: 'Obj'});    // 'Global'
fun1.bind({id: 'Obj'})();   // 'Global'
```
---
3. `arguments` 对象
- **普通函数**：内部有 `arguments` 对象，包含所有传入的实参（类数组）。
- **箭头函数**：**没有** `arguments` 对象。如果需要访问参数，使用**剩余参数 `...args`**。
```js
function fn1() { console.log(arguments) }  // ✅ 有 arguments
const fn2 = () => { console.log(arguments) }  // ❌ ReferenceError: arguments is not defined

// 箭头函数替代方案
const fn3 = (...args) => { console.log(args) }  // ✅ 剩余参数
```
---
4. 构造函数
- **普通函数**：可以作为构造函数，用 `new` 调用，创建实例。
- **箭头函数**：**不能**用作构造函数，没有 `prototype` 属性。
```js
function Person(name) { this.name = name }
const p = new Person('Alice')  // ✅

const Person2 = (name) => { this.name = name }
const p2 = new Person2('Bob')  // ❌ TypeError: Person2 is not a constructor
```
---
5. `prototype` 属性
- **普通函数**：有 `prototype` 属性，指向一个对象（用于原型继承）。
- **箭头函数**：**没有** `prototype` 属性。
```js
function fn1() {}
console.log(fn1.prototype)  // { constructor: f }

const fn2 = () => {}
console.log(fn2.prototype)  // undefined
```
---
6. 语法简洁
- 如果没有参数，就直接写一个空括号即可
- 如果只有一个参数，可以省去参数的括号
- 如果有多个参数，用逗号分割
- 如果函数体的返回值只有一句，可以省略大括号
- 如果函数体不需要返回值，且只有一句话，可以给这个语句前面加一个void关键字。最常见的就是调用一个函数：
```js
let fn = () => void doesNotReturn();
```
---
7. 不能用作生成器
	箭头函数不能使用 `yield` 关键字，因此不能用作**Generator**函数②。
---
#### this 的指向规则是什么？
`this` 的指向取决于函数的**调用方式**，而不是定义方式（箭头函数除外）。规则按优先级从高到低如下：
1. `new` 绑定（最高优先级）
	用 `new` 调用函数时，`this` 指向新创建的实例对象。
```js
function Person(name) {
  this.name = name
}
const p = new Person('Alice')
console.log(p.name) // 'Alice'
```
---
2. 显式绑定（`call`、`apply`、`bind`）
	手动指定 `this` 指向。
```js
function greet() { console.log(this.name) }
const obj = { name: 'Bob' }
greet.call(obj)  // 'Bob'
```
---
3. 隐式绑定（对象方法调用）
	函数作为对象的方法被调用时，`this` 指向该对象。
```js
const obj = {
  name: 'Carol',
  sayName() { console.log(this.name) }
}
obj.sayName() // 'Carol'
```
---
4. 默认绑定（普通函数调用）
	直接调用函数（非严格模式），`this` 指向全局对象（浏览器是 `window`，Node 是 `global`）；严格模式下为 `undefined`。
```js
function fn() { console.log(this) }
fn() // window（浏览器）或 global（Node）
```
---
5. 箭头函数（特殊规则）
	箭头函数没有自己的 `this`，它在**定义时**捕获外层作用域的 `this`，且**无法被任何方式改变**（包括 `call`、`apply`、`bind`）。
---
总结：判断顺序

> 1. 是不是 `new` 调用？ → 指向新对象
>     
> 2. 是不是箭头函数？ → 定义时外层 `this`，不能改
>     
> 3. 是不是 `call`/`apply`/`bind`？ → 指向指定对象
>     
> 4. 是不是对象方法？ → 指向该对象
>     
> 5. 都不是 → 严格模式 `undefined`，非严格模式全局对象
>
---
#### call、apply、bind 有什么区别？

	总结：
- **`call` 和 `apply`**：立即执行，区别在传参方式（逗号 vs 数组）。
- **`bind`**：不立即执行，返回新函数。

---
	`call`：立即执行，逐个传参
	**记忆口诀**：`call` —— **C** 开头，参数是 **C**omma（逗号）分隔的。
```js
function greet(greeting, punctuation) {
  console.log(greeting + ', ' + this.name + punctuation)
}

const person = { name: 'Alice' }

greet.call(person, 'Hello', '!')  // "Hello, Alice!"
```
---
	`apply`：立即执行，数组传参
	**记忆口诀**：`apply` —— **A** 开头，参数是 **A**rray（数组）。
```js
greet.apply(person, ['Hi', '.'])  // "Hi, Alice."
```

	实际场景：当参数本身就是一个数组时特别方便，比如数学函数：
```js
const nums = [5, 6, 2, 3, 7]
Math.max.apply(null, nums)  // 等价于 Math.max(5, 6, 2, 3, 7)
```
---
	`bind`：不立即执行，返回新函数
```js
const boundGreet = greet.bind(person, 'Hey')
boundGreet('!!')  // "Hey, Alice!!"
```

	实际场景：需要固定 `this` 但不立即调用的场合，比如事件回调、定时器：
---
### **异步编程：**

#### 对 Promise 的理解

解决问题：
	在 `Promise` 出现之前，异步操作依赖**回调函数**。多个异步任务需要依次执行时，会产生层层嵌套的**回调地狱**，代码难读、难维护。

---
Promise 的三种状态：
- **`pending`**
- **`fulfilled`**
- **`rejected`**
注意：一旦从进行状态变成为其他状态就永远不能更改状态了。
---
创建Promise对象：

Promise构造函数接受一个函数作为参数，该函数的两个参数分别是`resolve`和`reject`。
```js
const promise = new Promise(function(resolve, reject) {
  // ... some code
  if (/* 异步操作成功 */){
    resolve(value);
  } else {
    reject(error);
  }
});
```

一般情况下都会使用`new Promise()`来创建promise对象，但是也可以使用`promise.resolve`和`promise.reject`这两个方法：
```js
Promise.resolve(11).then(function(value){
  console.log(value); // 打印出11
});

Promise.reject(new Error(“我错了，请原谅俺！！”));
```
---
常用方法：

|方法|作用|
|---|---|
|**`Promise.then()`**|处理成功状态|
|**`Promise.catch()`**|处理失败状态|
|**`Promise.finally()`**|无论成功/失败都执行|
|`Promise.all([...])`|等待所有 Promise 成功，一个失败则全部失败|
|`Promise.allSettled([...])`|等待所有 Promise 完成，无论成功/失败|
|`Promise.race([...])`|返回最先完成的 Promise|
|`Promise.any([...])`|返回第一个成功的 Promise|
|`Promise.resolve()`|直接返回一个成功状态的 Promise|
|`Promise.reject()`|直接返回一个失败状态的 Promise|

---
#### async/await 的原理是什么？

原理实现：
async/await是`Generator` 的语法糖。

---
async/await对Promise的优化：
- 代码读起来更加同步，优化then链
- async/await传递中间值方便
- 错误处理友好，async/await使用try/catch方便
- 调试起来也比较方便
---
捕获异常：

```js
async function fn(){
    try{
        let a = await Promise.reject('error')
    }catch(error){
        console.log(error)
    }
}
```

---
#### 宏任务和微任务的区别

**只有异步任务才分为宏任务和微任务**

任务分类：

| 宏任务                        | 微任务                                                            |
| -------------------------- | -------------------------------------------------------------- |
| `setTimeout`、`setInterval` | promise 回调                                                     |
| DOM 事件（点击、键盘等）             | `MutationObserver`（监听 DOM 变化）                                  |
| `setImmediate`（Node.js）    | `queueMicrotask()`（一个全局函数，可以直接将一个任务放入**微任务队列**，在下一个宏任务之前立即执行。） |
| I/O 操作                     | `process.nextTick`（Node.js，优先级最高）                              |

---
#### 事件循环是怎样的？

┌───────────────────────────┐
│  执行同步代码（调用栈）                         │
│  ↓                                                            │
│  清空微任务队列                                      │  ← 微任务中产生的新微任务也会在这一轮清空
│  ↓                                                            │
│  判断是否需要渲染（如有）                     │
│  ↓                                                            │
│  从宏任务队列取出一个执行                     │  ← 每次只取一个宏任务
│  ↓                                                            │
│  回到“清空微任务队列”                             │  ← 循环往复
└───────────────────────────┘
**关键规则**：微任务队列必须**完全清空**（包括执行过程中新产生的微任务）后，才会执行下一个宏任务。

---
### **面向对象：**

#### 什么是原型？什么是原型链？

原型：

	在 JavaScript 里，每个对象在创建时都会关联另一个对象，这个被关联的对象就是“原型”。使用构造函数创建一个对象时，每一个构造函数的内部都有一个 prototype 属性，它的属性值是一个对象，这个对象包含了可以由该构造函数的所有实例共享的属性和方法。
	当使用构造函数新建一个对象后，在这个对象的内部将包含一个指针，指向构造函数的 prototype 属性对应的值，称为对象的原型（__proto__），这个已经是兼容性属性不推荐使用。最好用 Object.getPrototypeOf() 和 Object.setPrototypeOf() 方法替代。

---
原型链：

	当访问一个对象的属性时，如果这个对象内部不存在这个属性，那么它就会去它的原型对象里找这个属性，这个原型对象又会有自己的原型，于是就这样一直找下去，也就是原型链的概念。原型链的尽头一般来说都是 Object.prototype，但最终的终点是 null。
![image.png](https://raw.githubusercontent.com/Rongon/obsidian-note/main/20260810205740875.png)

---
原型修改和重写：
```js
function Person(name) {
  this.name = name
}

// 修改原型：在原有对象上添加方法
Person.prototype.sayHi = function() {
  console.log('Hi, I am ' + this.name)
}

// 修改已有方法
Person.prototype.sayHi = function() {
  console.log('Hello, I am ' + this.name)
}

const p1 = new Person('Alice')
p1.sayHi()  // 'Hello, I am Alice'
```

```js
function Person(name) {
  this.name = name
}

const p1 = new Person('Alice')

// 重写原型：整个换成新对象
Person.prototype = {
  sayHi() { console.log('Hi, I am ' + this.name) },
  sayBye() { console.log('Goodbye') }
}

// p1 是在重写前创建的，仍然指向旧原型，无法访问新方法
// p1.sayHi()  // ❌ TypeError: p1.sayHi is not a function

// 重写后创建的实例，指向新原型
const p2 = new Person('Bob')
p2.sayHi()  // ✅ 'Hi, I am Bob'

// constructor 丢失了
console.log(Person.prototype.constructor === Person)  // false
console.log(Person.prototype.constructor === Object)  // true

// 正确的重写方式：手动补回 constructor
Person.prototype = {
  constructor: Person,  // 手动指回
  sayHi() { console.log('Hi, I am ' + this.name) }
}
```

---
原型链指向：
```js
p.__proto__  // Person.prototype
Person.prototype.__proto__  // Object.prototype
p.__proto__.__proto__ //Object.prototype
p.__proto__.constructor.prototype.__proto__ // Object.prototype
Person.prototype.constructor.prototype.__proto__ // Object.prototype
p1.__proto__.constructor // Person
Person.prototype.constructor  // Person
```

---
#### JavaScript 如何实现继承？（5种）

- **核心目标**：所有方法都是为了**让属性能独立（不共享）**，同时让**方法能复用（共享在原型上）**。
- **最终答案**：`组合式继承` 是经典方案，`寄生组合式继承` 是最优方案，而 `class/extends` 就是它们的**语法糖**。
---
1. 原型链继承（最原始）
	**思路**：把父类的实例作为子类的原型。
```js
function Parent() {
  this.name = 'Parent'
  this.colors = ['red', 'blue']
}

function Child() {}

Child.prototype = new Parent()  // 关键：子类原型指向父类实例

const c1 = new Child()
const c2 = new Child()
c1.colors.push('green')
console.log(c2.colors)  // ['red', 'blue', 'green'] —— 出问题了！
```

**缺点**：
- 所有子类实例**共享**父类的引用类型属性（如数组），一个改了，其他都受影响。
- 创建子类实例时，**无法向父类构造函数传参**。

2. 构造函数继承（借用父类构造函数）
	**思路**：在子类构造函数中，用 `call` 调用父类构造函数，把父类属性复制一份到子类实例上。
```js
function Parent(name) {
  this.name = name
  this.sayName = function() {  // 方法在构造函数里
    console.log(this.name)
  }
}

function Child(name) {
  Parent.call(this, name)  // 把 Parent 里的代码执行一遍
}

const c1 = new Child('Alice')
const c2 = new Child('Bob')

// 每次 new Child 都创建了新的 sayName
console.log(c1.sayName === c2.sayName)  // false
```

**解决了**：引用类型共享问题 + 可以向父类传参。
**缺点**：方法必须定义在构造函数里，每个实例都会创建一份方法的副本，**不能共享方法**，无法复用。

3. 组合继承（原型链 + 构造函数，最经典）
	**思路**：属性用构造函数继承（保证每个实例独立），方法用原型链继承（方法共享）。
```js
function Parent(name) {
  this.name = name
  this.colors = ['red', 'blue']
}

Parent.prototype.sayName = function() {
  console.log(this.name)
}

function Child(name, age) {
  // ★ 第二次调用 Parent：给子类实例添加 name 和 colors 属性
  Parent.call(this, name)
  this.age = age
}

// ★ 第一次调用 Parent：给子类原型添加 name 和 colors 属性（多余的！）
Child.prototype = new Parent()
Child.prototype.constructor = Child

const c1 = new Child('Alice', 18)

// 实例自身有 name 和 colors（来自第二次调用 Parent）
console.log(c1.name)   // 'Alice'
console.log(c1.colors) // ['red', 'blue']

// ❌ 子类原型上也有 name 和 colors（来自第一次调用 Parent），完全是多余的
console.log(Child.prototype.name)   // undefined —— 多余的属性
console.log(Child.prototype.colors) // ['red', 'blue'] —— 多余的属性

// 实例自身的属性遮盖了原型上的同名属性
// c1.name 访问的是实例自身的 'Alice'，而不是原型上的 undefined
// c1.colors 访问的是实例自身的 ['red', 'blue']，而不是原型上的 ['red', 'blue']

// 验证：用 hasOwnProperty 区分自身属性和原型属性
console.log(c1.hasOwnProperty('name'))   // true —— 自身属性
console.log(c1.hasOwnProperty('colors')) // true —— 自身属性
// 原型上的 name 和 colors 被实例属性遮盖，根本用不到，却白白占着内存
```

**优点**：属性独立 + 方法共享，是 ES6 之前最常用的继承方式。
**缺点**：父类构造函数被调用了**两次**，子类原型上多了一份多余的父类实例属性。

4. 寄生组合式继承（最优解）
	**思路**：用一个空函数作为中介，只继承父类的原型方法，不调用父类构造函数创建多余属性。
```js
function Parent(name) {
  this.name = name
  this.colors = ['red', 'blue']
}

Parent.prototype.sayName = function() {
  console.log(this.name)
}

function Child(name, age) {
  Parent.call(this, name)  // 只调用一次 Parent
  this.age = age
}

// 关键：用 Object.create 直接继承父类原型
Child.prototype = Object.create(Parent.prototype)
Child.prototype.constructor = Child

const c1 = new Child('Alice', 18)
c1.sayName()  // 'Alice'
```

**优点**：父类构造函数只调用一次，原型链上不产生多余属性，是 ES6 之前最完美的继承方式。

5. ES6 class 继承（现代标准写法）
	**思路**：用 `class` 和 `extends` 关键字，本质上是寄生组合式继承的语法糖。
```js
class Parent {
  constructor(name) {
    this.name = name
    this.colors = ['red', 'blue']
  }
  
  sayName() {
    console.log(this.name)
  }
}

class Child extends Parent {
  constructor(name, age) {
    super(name)  // 必须调用 super，相当于 Parent.call(this, name)
    this.age = age
  }
}

const c1 = new Child('Alice', 18)
c1.sayName()  // 'Alice'
```

**关键点**：
- `extends`：建立原型链。
- `super()`：在子类构造函数中必须先调用，它指向父类的构造函数。
- `sayName` 这类方法会自动挂载到父类原型上，子类实例通过原型链访问。

---
#### ES6 的 class 和 ES5 的构造函数有什么区别？

ES6 的 `class` 本质上是 ES5 构造函数写法的语法糖，它让代码更清晰、更安全，但底层原理仍然是原型链继承。

两者的主要区别在于：`class` 语法更简洁优雅；`class` 内部定义的方法自动不可枚举；`class` 内部自动启用严格模式，并且不存在变量提升；`class` 强制必须用 `new` 调用，避免了构造函数在 ES5 中可能被直接调用导致的 `this` 指向问题。

在继承方面，`class` 通过 `extends` 和 `super()` 关键字，用更直观的方式实现了 ES5 中复杂的寄生组合式继承，开发者不再需要手动处理原型链和构造函数指向。需要注意的是，`class` 虽然看起来像传统面向对象语言，但它本质上还是基于 JavaScript 原型的继承模型。

---
### **其他：**

#### 如何实现深拷贝？
1. `JSON.stringify` + `JSON.parse`（最简单）
	**适用场景**：仅适用于纯数据对象（只有基本类型、普通对象和数组），且没有特殊类型和循环引用的简单场景。

```js
const obj = { a: 1, b: { c: 2 } }
const clone = JSON.parse(JSON.stringify(obj))
```

**优点**：一行代码搞定，简单快捷。
**缺点**：
- **无法处理函数**：函数会被直接丢弃，`clone` 里不会有这个方法。
    
- **无法处理 `undefined`**：值为 `undefined` 的属性会被直接删除。
    
- **无法处理 `Symbol`**：`Symbol` 类型的键值对会被忽略。
    
- **无法处理循环引用**：对象里如果有属性引用了自身，会直接报错。
    
- **无法处理特殊对象**：`Date` 会变成字符串，`RegExp` 会变成空对象，`Map`、`Set`、`Blob` 等更是无法正确处理。
---
2. 手写递归深拷贝（面试使用）
```js
function deepClone(obj, map = new WeakMap()) {
  // 基本类型直接返回
  if (obj === null || typeof obj !== 'object') return obj
  
  // 处理循环引用：如果这个对象已经被拷贝过，直接返回保存的副本
  if (map.has(obj)) return map.get(obj)
  
  // 处理日期
  if (obj instanceof Date) return new Date(obj)
  
  // 处理正则
  if (obj instanceof RegExp) return new RegExp(obj)
  
  // 处理数组和对象
  const cloneObj = Array.isArray(obj) ? [] : {}
  map.set(obj, cloneObj)  // 先保存引用，再递归拷贝属性
  
  for (let key in obj) {
    if (obj.hasOwnProperty(key)) {
      cloneObj[key] = deepClone(obj[key], map)
    }
  }
  
  return cloneObj
}
```

**缺点**：
- 需要对各种数据类型做判断，逻辑相对复杂。
---
3. `structuredClone()`（现代浏览器原生）
	**适用场景**：现代浏览器环境、不需要拷贝函数和 `Symbol` 的场景。
```js
const obj = { a: 1, b: { c: 2 }, date: new Date() }
const clone = structuredClone(obj)
```

**优点**：

- 浏览器原生支持，无需手写或引用库。
- 可以正确处理 `Date`、`RegExp`、`Map`、`Set`、`ArrayBuffer`、`Blob` 等复杂类型。
- 支持循环引用，不会报错。

**缺点**：

- **无法处理函数**：函数会被直接报错（`DataCloneError`）。
- **无法处理 `Symbol`**。
- **无法处理 DOM 节点**。
- 兼容性相对较新（Chrome 98+、Node 17+），老旧浏览器不支持。
---
4. Lodash `cloneDeep`（第三方库）
```js
import { cloneDeep } from 'lodash-es'

const obj = { a: 1, b: { c: 2 }, fn: () => {} }
const clone = cloneDeep(obj)
```

**优点**：经过大量项目验证，处理了几乎所有边界情况，是最可靠的方案。
**缺点**：需要引入额外的库，增加项目体积。
**适用场景**：项目中已经使用了 Lodash，或者需要处理极其复杂的对象结构时。

---

#### 什么是防抖和节流？如何实现？

防抖和节流是前端性能优化中用于**控制函数执行频率**的两种常用手段，主要用于处理高频触发的事件，如 `scroll`、`resize`、`input`、`mousemove` 等。

---
防抖：

**概念**：在事件被连续触发时，防抖会不断重置计时器，只有当用户停止触发一段时间后，函数才会执行一次。可以理解为“等你不动了再干”。

**适用场景**：
- 输入框实时搜索（等待用户输入完再发请求）
- 窗口大小 `resize` 回调
- 按钮提交防重复点击

**基本实现**：
```js
function debounce(fn, delay) {
  let timer = null                          // ① 闭包变量：保存定时器 ID

  return function (...args) {               // ② 返回新函数（闭包）
    clearTimeout(timer)                     // ③ 每次触发，清除上次的定时器
    timer = setTimeout(() => {              // ④ 重新设置定时器
      fn.apply(this, args)                  // ⑤ 延迟结束后执行原函数
    }, delay)
  }
}

const handleInput = debounce((e) => {
  console.log('搜索:', e.target.value);
}, 300);
input.addEventListener('input', handleInput);
```

**带立即执行选项的实现**（有时需要第一次触发时立即执行，后续防抖）：
```js
function debounce(fn, delay, immediate = false) {
  let timer = null                          // ① 闭包变量：保存定时器 ID

  return function (...args) {               // ② 返回新函数（闭包）
    const callNow = immediate && !timer     // ③ 判断是否立即执行

    clearTimeout(timer)                     // ④ 清除上次定时器

    timer = setTimeout(() => {              // ⑤ 设置新定时器
      timer = null                          // ⑥ 定时器到期：清空标记
      if (!immediate) fn.apply(this, args)  // ⑦ 非立即模式下，到期后执行
    }, delay)

    if (callNow) fn.apply(this, args)       // ⑧ 立即模式且首次触发，立刻执行
  }
}
```
---
节流：
**概念**：无论事件触发有多频繁，节流会保证在固定的时间间隔内最多执行一次函数。可以理解为“到点了就干一次”。

**适用场景**：
- 滚动加载更多（`scroll`）
- 鼠标移动事件（`mousemove`）
- 页面元素拖拽

**基本实现（时间戳版）**：
```js
function throttle(fn, delay) {
  let lastTime = 0                          // ① 记录上次执行的时间戳

  return function (...args) {               // ② 返回新函数（闭包）
    const now = Date.now()                  // ③ 获取当前时间戳

    if (now - lastTime >= delay) {          // ④ 判断是否超过间隔
      fn.apply(this, args)                  // ⑤ 执行原函数
      lastTime = now                        // ⑥ 更新最后执行时间
    }
  }
}

const handleScroll = throttle(() => {
  console.log('滚动位置:', window.scrollY);
}, 200);
window.addEventListener('scroll', handleScroll);
```

**定时器版**：
```js
function throttle(fn, delay) {
  let timer = null                          // ① 闭包变量：保存定时器 ID

  return function (...args) {               // ② 返回新函数（闭包）
    if (timer) return                       // ③ 如果已有等待中的任务，直接忽略本次触发

    timer = setTimeout(() => {              // ④ 设置定时器
      fn.apply(this, args)                  // ⑤ 延迟后执行原函数
      timer = null                          // ⑥ 清空标记，允许下次触发
    }, delay)
  }
}
```

**记忆口诀**：防抖是“等你不动”，节流是“到点就动”。

---
#### 什么是事件委托？

事件委托本质上是利用了**浏览器事件冒泡**的机制。

应用场景：
- **无限滚动列表**：对列表容器进行委托，处理每个列表项的点击事件。
    
- **动态表格操作**：表格的行经常增删，在`<tbody>`上委托“编辑”、“删除”等按钮事件。
    
- **全局事件监听**：在 `document` 或 `document.body` 上委托事件，以达到全局统一管理的目的。

优化点：
- **性能优化**：当有成百上千个子元素需要监听时，为每个子元素单独绑定事件会消耗大量内存。事件委托只需要**绑定一个事件处理器**，内存占用与子元素数量无关。
    
- **动态元素监听**：对于未来通过JavaScript动态添加的元素，直接绑定的事件不会生效。而事件委托因为监听的是父元素，所以对**任何时刻添加**的子元素都能自动响应，无需手动重新绑定。

vue示例：
```vue
<template>
  <!-- 父容器监听 click 事件，实现事件委托 -->
  <ul @click="handleClick">
    <li v-for="item in items" :key="item.id" :data-id="item.id">
      {{ item.text }}
      <button>删除</button>
    </li>
  </ul>
</template>

<script setup>
import { ref } from 'vue'

const items = ref([
  { id: 1, text: '项目 1' },
  { id: 2, text: '项目 2' },
  { id: 3, text: '项目 3' }
])

function handleClick(event) {
  // 1. 通过 event.target 获取实际被点击的元素
  const target = event.target
  
  // 2. 判断是否点击了某个特定子元素（例如 button）
  if (target.tagName === 'BUTTON') {
    // 3. 从最近的 <li> 中获取数据 id
    const li = target.closest('li')
    const id = li.dataset.id
    console.log('删除项目 ID:', id)
  }
}
</script>
```

react示例：
```jsx
import React, { useState } from 'react'

function App() {
  const [items, setItems] = useState([
    { id: 1, text: '项目 1' },
    { id: 2, text: '项目 2' },
    { id: 3, text: '项目 3' }
  ])

  // 事件委托处理函数
  const handleClick = (event) => {
    const target = event.target
    // 判断点击的是按钮
    if (target.tagName === 'BUTTON') {
      const li = target.closest('li')
      const id = li.dataset.id
      console.log('删除项目 ID:', id)
    }
  }

  return (
    <ul onClick={handleClick}>
      {items.map(item => (
        <li key={item.id} data-id={item.id}>
          {item.text}
          <button>删除</button>
        </li>
      ))}
    </ul>
  )
}

export default App
```
---
# ts

## 经典面试题

### TypeScript 和 JavaScript 有什么区别？TypeScript 的优势是什么？

TypeScript 与 Js 核心区别在于 TypeScript 增加了**静态类型系统**，能在编译时提前发现类型错误，而不像 JavaScript 只能在运行时暴露问题。

TypeScript 的优势主要体现在：静态类型检查能提前发现错误；编辑器智能提示能显著提升开发效率；类型定义本身就是代码的最佳文档，降低团队协作和维护成本；重构时编译器会自动提示所有受影响的代码，安全可靠。

---
### interface 和 type 有什么区别？什么时候用 interface，什么时候用 type？

1. 定义范围不同
	`type` 可以定义 `interface` 无法表示的类型：
```ts
// type 可以定义原始类型别名
type Name = string

// type 可以定义联合类型
type Status = 'loading' | 'success' | 'error'

// type 可以定义元组
type Point = [number, number]

// type 可以定义交叉类型
type A = { a: number }
type B = { b: string }
type AB = A & B

// interface 主要定义对象形状
interface User {
  name: string
  age: number
}
```

2. 扩展方式不同
	两者都可以扩展，但 `interface` 的 `extends` 在复杂类型时更易读。
```ts
// interface 使用 extends
interface Animal { name: string }
interface Dog extends Animal { breed: string }

// type 使用交叉类型 &
type Animal = { name: string }
type Dog = Animal & { breed: string }
```

3. 声明合并（interface 独有）
	声明合并非常适合**扩展第三方库的类型**，比如给 `Window` 或 `Vue Router` 的 `RouteMeta` 添加自定义属性。
```ts
// interface 同名会自动合并
interface User {
  name: string
}
interface User {
  age: number
}
// 最终 User = { name: string; age: number }

// type 重复定义会报错
type User = { name: string }
type User = { age: number }  // ❌ 错误：标识符 "User" 重复
```

4. 映射类型与高级类型（type 更强）
```ts
// type 可以使用映射类型
type Readonly<T> = {
  readonly [P in keyof T]: T[P]
}

// type 可以使用条件类型
type IsString<T> = T extends string ? true : false

// interface 不能直接实现这些
```
---
优先使用 `interface` 的场景：

- **定义对象/类/函数的形状**（最常用）
- **需要被继承或实现**（`extends`、`implements`）
- **需要声明合并**（如扩展第三方库的类型）
```js
interface User {
  name: string
  age: number
}

interface Admin extends User {
  role: string
}

class Person implements User {
  name = 'Alice'
  age = 30
}
```

必须使用 `type` 的场景：

- **定义原始类型别名**（`type Name = string`）
- **定义联合类型**（`type Status = 'loading' | 'success'`）
- **定义元组**（`type Point = [number, number]`）
- **使用映射类型、条件类型等高级类型**
- **需要交叉类型组合多个类型**
```ts
type Status = 'loading' | 'success' | 'error'
type Point = [number, number]
type PartialUser = Partial<User>
```
---
### any、unknown、never 三种类型有什么区别？

- `any`：最宽松，放弃检查

**特点**：相当于告诉 TypeScript“这个变量你别管了”，完全跳过类型检查。

**使用场景**：
- 快速原型、紧急修复
- 渐进式迁移旧 JS 项目

**风险**：**完全失去类型保护**，错误只能在运行时暴露。

---
- `unknown`：未知但安全

**特点**：表示“我现在不知道它是什么类型”，必须先**类型收窄**后才能使用。

**使用场景**：
- API 返回值不确定结构时
- 处理 `JSON.parse()` 的结果
- `catch` 捕获的错误（默认 `unknown`）
- 任何“我不确定类型但想保持安全”的场景

**优势**：**安全**，强制你在使用前确认类型，避免运行时错误。

---
- `never`：不可能存在的类型

**特点**：表示“永远不会有值”。是所有类型的**子类型**，但没有任何类型可以赋给它。

**使用场景**：
- 函数永远不会返回（抛错、死循环）
- 穷尽性检查（确保覆盖所有联合类型分支）
- 作为其他类型的子类型，可用于类型运算
---
### 什么是泛型？如何使用泛型约束？

总结：

泛型是 TypeScript 中一种让类型也变成“参数”的机制，允许我们在定义函数、接口、类时不指定具体类型，而在使用时再确定。这样既能保持类型安全，又能提高代码复用性。

泛型约束则是通过 `extends` 关键字来限制泛型类型的范围，比如 `T extends HasLength` 要求 `T` 必须拥有 `length` 属性。常见的约束包括指定接口、使用 `keyof` 限制为对象的键、以及用交叉类型同时满足多个约束。

---
- 泛型：
	泛型是 TypeScript 中一种**在定义时不指定具体类型，而在使用时才确定类型**的机制。它让函数、接口、类等能够适用于多种类型，同时保持类型安全。

	简单说：**泛型就是“类型的参数”。**

```ts
// 没有泛型时，只能用 any，失去类型安全
function identity(value: any): any {
  return value
}

// 使用泛型：T 是一个类型变量，调用时才确定具体类型
function identity<T>(value: T): T {
  return value
}

const s = identity('hello')   // s 的类型被推断为 string
const n = identity(42)        // n 的类型被推断为 number
```
`<T>` 就是泛型参数，`T` 是名字（可以叫任何名字，如 `U`、`Type`），它在函数调用时被具体的类型替换。

---
常用场景：
1. 函数泛型
```ts
function getFirst<T>(arr: T[]): T | undefined {
  return arr[0]
}

getFirst([1, 2, 3])       // T = number
getFirst(['a', 'b', 'c']) // T = string
```

2. 接口泛型
```ts
interface ApiResponse<T> {
  data: T
  code: number
  message: string
}

// 使用时要指定具体类型
const userResponse: ApiResponse<{ name: string }> = {
  data: { name: 'Alice' },
  code: 200,
  message: 'ok'
}
```

3. 类泛型
```ts
class Stack<T> {
  private items: T[] = []
  
  push(item: T) { this.items.push(item) }
  pop(): T | undefined { return this.items.pop() }
}

const numberStack = new Stack<number>()
numberStack.push(1)
numberStack.push(2)
```

---
- 泛型约束：
	默认情况下，泛型变量 `T` 可以是任何类型。但有时候我们希望限制它必须具备某些属性或方法，这时就需要**泛型约束**。

	使用 `extends` 关键字来约束泛型类型。

1. 基本约束
```ts
// 约束 T 必须包含 length 属性
function getLength<T extends { length: number }>(value: T): number {
  return value.length
}

getLength('hello')        // ✅ string 有 length
getLength([1, 2, 3])      // ✅ array 有 length
getLength(42)             // ❌ number 没有 length，编译报错
```

2. 使用接口定义约束
```ts
interface HasLength {
  length: number
}

function logLength<T extends HasLength>(value: T): void {
  console.log(value.length)
}
```

3. 使用 keyof 约束泛型参数为对象的键
	这里 `K extends keyof T` 表示 `K` 必须是 `T` 类型的键名之一。
```ts
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key]
}

const person = { name: 'Alice', age: 30 }
getProperty(person, 'name')  // ✅ 返回 string
getProperty(person, 'age')   // ✅ 返回 number
getProperty(person, 'email') // ❌ 'email' 不是 person 的键，编译报错
```

4. 多个约束（交叉类型）
```ts
interface HasId {
  id: number
}
interface HasName {
  name: string
}

// T 必须同时满足 HasId 和 HasName
function process<T extends HasId & HasName>(item: T): void {
  console.log(item.id, item.name)
}
```
---
### TypeScript 的工具类型 Partial、Pick、Omit 是如何实现的？

总结：

这三个工具类型都是通过**映射类型**实现的。

`Partial<T>` 的定义是 `{ [P in keyof T]?: T[P] }`，它遍历 `T` 的所有属性，然后把每个属性都变成可选属性。

`Pick<T, K>` 的定义是 `{ [P in K]: T[P] }`，其中 `K extends keyof T`，它从 `T` 中挑选出 `K` 指定的那些属性来构造新类型。

`Omit<T, K>` 则是 `Pick<T, Exclude<keyof T, K>>`，它先用 `Exclude` 从 `T` 的所有键中排除 `K` 包含的键，再用 `Pick` 选出剩余的键。它的作用是从类型中删除某些属性。

---
1. `Partial<T>`：让所有属性变为可选
	**实现：**
```ts
type Partial<T> = {
  [P in keyof T]?: T[P]
}
```

**拆解：**
- `keyof T`：获取 `T` 所有键名的联合类型。
- `[P in keyof T]`：遍历 `T` 的每一个键，`P` 是每个键名。
- `?:`：把每个属性变为可选属性。
- `T[P]`：索引访问类型，获取键 `P` 对应的值类型。

**示例：**
```ts
interface User {
  name: string
  age: number
}

type PartialUser = Partial<User>
// 结果：{ name?: string; age?: number }
```
---
2. `Pick<T, K>`：从类型中挑选部分属性
	**实现：**
```ts
type Pick<T, K extends keyof T> = {
  [P in K]: T[P]
}
```

**拆解：**
- `K extends keyof T`：约束 `K` 必须是 `T` 的键名。
- `[P in K]`：遍历 `K` 中的每个键（`K` 是联合类型）。
- `T[P]`：从 `T` 中取出对应键的值类型。

**示例：**
```ts
interface User {
  name: string
  age: number
  email: string
}

type UserPreview = Pick<User, 'name' | 'email'>
// 结果：{ name: string; email: string }
```
---
3. `Omit<T, K>`：从类型中排除部分属性
	**实现：**
```ts
type Omit<T, K extends keyof any> = Pick<T, Exclude<keyof T, K>>
```

**拆解：**
- `Exclude<keyof T, K>`：从 `T` 的所有键中剔除 `K` 包含的键，得到剩余的键名联合类型。
- `Pick<T, ...>`：从 `T` 中挑选这些剩余的键，组成新类型。

`Exclude<U, V>` 本身也是一个内置工具类型，定义是：
```ts
type Exclude<U, V> = U extends V ? never : U
```
它把联合类型 `U` 中能赋给 `V` 的成员剔除掉。

**示例：**
```ts
interface User {
  name: string
  age: number
  email: string
}

type UserWithoutEmail = Omit<User, 'email'>
// 结果：{ name: string; age: number }
```

**注意：** 标准库中 `Omit` 也可以直接写成：
```ts
type Omit<T, K extends keyof any> = {
  [P in Exclude<keyof T, K>]: T[P]
}
```
两者等价，但官方实现用了 `Pick` 组合，更简洁。

---
共同依赖的核心类型操作：

| 特性                 | 作用                              |
| ------------------ | ------------------------------- |
| **`keyof T`**      | 获取 `T` 所有键名的联合类型                |
| **`[P in K]`**     | 映射类型语法，遍历联合类型 `K` 的每个成员         |
| **`T[P]`**         | 索引访问类型，获取键 `P` 对应的值类型           |
| **`extends` 约束**   | 限制类型参数的范围，如 `K extends keyof T` |
| **`?` 可选修饰符**      | 映射类型中让属性变为可选                    |
| **条件类型 + `never`** | 用于 `Exclude`，剔除联合类型中的部分成员       |

---
### 如何在 Vue/React 项目中使用 TypeScript？


---
### tsconfig.json 中的 strict 选项包含哪些子选项？

`tsconfig.json` 中的 `"strict": true` 是一个复合选项，开启后相当于同时启用以下 7 个严格检查选项：

1. `noImplicitAny`

- **作用**：禁止隐式 `any` 类型。
    
- **表现**：函数参数、变量等如果没有显式标注类型，且无法被推断时，会直接报错。
    
- **示例**：`function foo(param) {}` 会报错，必须写成 `function foo(param: string) {}`。
---
2. `noImplicitThis`

- **作用**：禁止 `this` 隐式 `any` 类型。
    
- **表现**：在函数或类方法中使用 `this` 时，如果上下文无法推断出 `this` 的类型，会报错。
---
3. `alwaysStrict`

- **作用**：始终以严格模式解析代码，并在生成的 JavaScript 文件顶部添加 `"use strict"`。
    
- **表现**：启用后，代码将遵循 ES5 严格模式的规则。
---
4. `strictBindCallApply`

- **作用**：对 `bind`、`call`、`apply` 的参数类型进行严格检查。
    
- **表现**：调用这些方法时，传入的参数必须与目标函数签名匹配，否则报错。
---
5. `strictNullChecks`

- **作用**：将 `null` 和 `undefined` 视为独立的类型，不再允许它们赋给其他类型（如 `string`、`number`）。
    
- **表现**：除非使用联合类型（如 `string | null`），否则 `null`/`undefined` 不能赋值。这是最重要的严格检查之一。
---
6. `strictFunctionTypes`

- **作用**：对函数参数类型进行**逆变**检查。
    
- **表现**：在类型系统中，函数参数类型必须满足严格的子类型关系，防止不安全的函数赋值。
---
7. `strictPropertyInitialization`

- **作用**：要求类的属性必须在构造函数中初始化，或者使用 `!` 断言明确表示不初始化。
    
- **表现**：如果属性未在声明时或构造函数中赋值，且未标记为可选或 `!`，会报错。⚠️ 该选项需要同时开启 `strictNullChecks` 才能生效。
---
补充说明：

- 从 TypeScript 4.4 开始，`useUnknownInCatchVariables` 也默认为 `true`（`catch` 捕获的变量默认为 `unknown`），但它**不是** `strict` 的子选项，而是独立默认开启的。
    
- 在 `tsconfig.json` 中，如果你只想开启部分严格检查，可以单独设置这些选项，而不使用 `"strict": true`。
---
### 如何为第三方 JavaScript 库编写类型声明文件？

总结：

为第三方 JavaScript 库编写类型声明文件，通常就是创建一个 `.d.ts` 文件，使用 `declare module '库名'` 来声明模块，然后在内部用 `export` 描述该库的 API，比如导出函数、接口、类、常量等。需要根据库的模块格式选择 `export default` 还是 `export =`。然后把文件放到项目的 `types` 目录，并确保被 `tsconfig.json` 包含。

如果是全局库，可以用 `declare global` 扩展全局对象。如果只是想快速解决报错，写一个简写声明 `declare module '库名';` 就行，但会丢失类型安全。

实际开发中，优先检查是否有 `@types` 包，如果没有再手写。手写时要尽量精确描述 API，可以用函数重载、接口合并等特性，让类型提示更好用。

---
核心步骤：

1. 创建一个 `.d.ts` 文件

	通常放在项目的 `types/` 或 `src/types/` 目录下，并确保该目录被 `tsconfig.json` 的 `include` 包含。

---
2. 使用 `declare module` 声明模块

	对于 npm 包，使用模块声明来覆盖整个库。
```ts
// types/my-library.d.ts
declare module 'my-library' {
  // 在这里描述库的 API
}
```
---
3. 在模块内部描述类型

	根据库的实际 API，使用 `export` 声明函数、接口、类、常量等。
```ts
declare module 'my-library' {
  export interface Options {
    retry?: number
    timeout?: number
  }

  export function createClient(options: Options): Client

  export class Client {
    get(url: string): Promise<any>
    post(url: string, data: any): Promise<any>
  }

  export const version: string
}
```
---
4. 处理默认导出和 CommonJS 导出

- **ES Module 默认导出**：
```ts
declare module 'my-library' {
  export default function main(param: string): void
}
```

- **CommonJS 模块（`module.exports = ...`）**：
```ts
declare module 'my-library' {
  function main(param: string): void
  export = main
}
```

- **同时有具名和默认导出**：
```ts
declare module 'my-library' {
  export default function main(param: string): void
  export function helper(): void
}
```

---
5. 处理命名空间（全局库）

	如果库暴露为一个全局变量（如 jQuery 的 `$`），使用 `declare global` 或全局声明：
```ts
declare global {
  interface Window {
    myGlobalLibrary: any
  }
}

// 或者直接声明全局变量
declare var myGlobalLibrary: any
```
---
6. 快速声明（所有类型为 `any`）

	如果只是想快速让项目不报错，可以写一个简写声明：
```ts
declare module 'my-library'
```

这样库的所有导出都是 `any`，失去了类型安全，但暂时可用。

---
示例：为 `lodash` 的一部分编写声明

假设有一个简单的库 `math-utils`，源码可能是这样：
```ts
// math-utils.js
export function add(a, b) {
  return a + b
}

export function multiply(a, b) {
  return a * b
}

export const PI = 3.14159

export default function sum(numbers) {
  return numbers.reduce((a, b) => a + b, 0)
}
```

对应的声明文件：
```ts
// types/math-utils.d.ts
declare module 'math-utils' {
  export function add(a: number, b: number): number
  export function multiply(a: number, b: number): number
  export const PI: number

  export default function sum(numbers: number[]): number
}
```
---
如何让 TypeScript 找到声明文件：

1. **直接放在项目源码中**：确保 `.d.ts` 文件在 `tsconfig.json` 的 `include` 范围内（如 `src/**/*.d.ts`）。
    
2. **使用 `typeRoots` 指定目录**：在 `tsconfig.json` 中设置 `typeRoots`，然后在该目录下为每个库建一个文件夹。
    
3. **安装 `@types` 包**：很多库已经有社区维护的类型，直接 `npm install -D @types/xxx`。
    
4. **发布到 DefinitelyTyped**：如果是为开源社区做贡献，可以将声明文件提交到 DefinitelyTyped 仓库。
---
最佳实践和注意事项：

- **准确描述 API**：了解库的实际参数和返回值，避免用 `any` 滥用。
    
- **支持声明合并**：如果需要扩展第三方库已有的类型，使用 `interface` 而不是 `type`，因为接口可以合并。
    
- **处理重载**：如果函数有多个签名，使用函数重载。
    
- **测试声明文件**：写一个 `test.ts` 文件，导入库并使用，运行 `tsc --noEmit` 查看是否报错。
    
- **逐步完善**：可以先用简写 `any`，再逐步补全类型。
---
# vue
## 经典面试题

### **基础概念：**

#### Vue 有什么特点？

Vue 最大的特点是**渐进式**。它不像 React 那样需要一个完整的生态才能发挥作用，可以只用一个 script 标签引入 Vue 做一个小交互，也可以用它配合 Router、Pinia、Vite 构建大型应用。

第二点是**响应式系统**。Vue 的响应式是自动依赖追踪的，只需要声明数据，模板里用到这个数据的地方会自动更新，不需要像 React 那样手动优化或用 memo。

第三点是**开发体验**。Vue 的单文件组件把模板、逻辑、样式放在一起，结构清晰；模板语法接近 HTML，学习成本低，团队协作时上手快。

我个人在项目中感受最深的是它的响应式和易用性，让我能更专注业务逻辑，而不用过多关心底层渲染。

---
#### MVVM 模式是什么？

总结：
MVVM 是一种前端架构模式，分为 Model（数据）、View（视图）、ViewModel（视图模型）三层。它的核心是 ViewModel 作为中间桥梁，实现数据和视图的双向自动同步：数据变了视图自动更新，视图输入也自动写回数据。

在 Vue 中，模板是 View，组件内的 `ref`/`data` 是 Model，而 Vue 的响应式系统扮演 ViewModel 的角色。比如 `v-model` 同时处理了 View 到 Model 的输入监听和 Model 到 View 的数据渲染。

对比 MVC，MVVM 减少了手动操作 DOM 的代码，降低了视图和业务的耦合，让开发更关注数据本身。

---
三个组成：

|层|职责|前端中的对应|
|---|---|---|
|**Model**|数据与业务逻辑，负责存储数据和操作数据（如调用 API）|Vue 组件中的 `data`/`ref`、Pinia/Redux 的 store|
|**View**|用户界面，负责展示数据、监听用户操作|HTML 模板 / JSX|
|**ViewModel**|连接 Model 和 View 的桥梁，处理视图逻辑，将 Model 数据映射到 View，并监听 View 变化更新 Model|Vue 实例 / 组件实例（响应式系统）|

---
核心机制：双向绑定：

MVVM 最大的特点是**双向数据绑定**：

- **Model → View**：数据变化自动反映到视图（不用手动操作 DOM）。
    
- **View → Model**：用户在视图上的输入自动更新数据（如 `v-model` 监听 `input` 事件）。

这个“自动”就是 ViewModel 在起作用。它通过响应式系统追踪依赖，在数据变化时更新 DOM，在 DOM 事件触发时更新数据。

---
与 MVC 的区别：

|模式|数据流向|特点|
|---|---|---|
|**MVC**|单向：View → Controller → Model → View|Controller 需要手动更新 View 和 Model|
|**MVVM**|双向：View ↔ ViewModel ↔ Model|ViewModel 自动同步，无需手动操作 DOM|

在传统 MVC 中，Controller 需要写大量 DOM 操作代码来同步视图和数据；而 MVVM 把这件事交给框架的响应式系统，开发者只需声明数据绑定关系。

---
MVVM 的优点：

- **降低耦合**：View 和 Model 不直接通信，便于独立开发和测试。
    
- **减少 DOM 操作**：开发者专注数据，框架自动处理 DOM 更新。
    
- **提高可维护性**：视图逻辑（ViewModel）与业务逻辑（Model）分离。

---
#### Vue 的响应式原理是什么？

总结：
Vue 的响应式原理本质上是数据劫持 + 依赖收集 + 派发更新。

Vue 3 使用 Proxy 来劫持整个对象。当数据被读取时，会触发 `get`，在 `get` 里做依赖收集（`track`），记录下当前正在执行的副作用函数（比如组件渲染函数）依赖了这个数据。当数据被修改时，会触发 `set`，在 `set` 里做派发更新（`trigger`），通知所有依赖这个数据的副作用函数重新执行。

组件渲染函数本身就是一个副作用函数。所以数据变化时，渲染函数会自动重新执行，更新视图。

---
Vue 3 使用 **Proxy** 来劫持整个对象的所有操作。

1. 数据劫持（Proxy）
```js
function reactive(obj) {
  return new Proxy(obj, {
    get(target, key) {
      track(target, key)   // 读取时收集依赖
      return target[key]
    },
    set(target, key, value) {
      target[key] = value
      trigger(target, key) // 修改时触发更新
      return true
    }
  })
}
```

- **`get`**：当数据被读取时，调用 `track` 记录“谁在用这个数据”。
    
- **`set`**：当数据被修改时，调用 `trigger` 通知所有依赖这个数据的地方重新执行。
---
2. 依赖收集（track）与派发更新（trigger）
	Vue 内部维护一个全局的依赖仓库，结构大致是：

```
WeakMap<目标对象, Map<属性名, Set<副作用函数>>>
```

- **`track(target, key)`**：把当前正在执行的副作用函数（比如组件的渲染函数、`computed`、`watch`）添加为这个属性的订阅者。
    
- **`trigger(target, key)`**：找到这个属性的所有订阅者，并让它们重新执行。

---
3. 副作用函数（effect/render）

组件的渲染函数本身就是一个副作用函数。组件首次渲染时，会读取模板中用到的所有响应式数据，这些读取操作触发 `track`，从而把“渲染函数”记录为数据的依赖。之后数据变化，`trigger` 就会通知渲染函数重新执行，更新视图。

---
Vue 2 的响应式实现（对比）
	Vue 2 使用 **`Object.defineProperty`** 来劫持对象属性的 `getter` 和 `setter`。
```js
function defineReactive(obj, key, val) {
  Object.defineProperty(obj, key, {
    get() {
      // 依赖收集
      return val
    },
    set(newVal) {
      // 派发更新
      val = newVal
    }
  })
}
```

**Vue 2 的局限**：

- 无法监听属性的**新增和删除**，需要通过 `Vue.set` / `this.$set` 来处理。
    
- 对数组的监听不完善，需要重写数组的 7 个方法（`push`、`pop` 等）来触发更新。
    
- 需要递归遍历每个属性进行劫持，初始化性能较差。

**Vue 3 的改进**：

- 用 **Proxy** 拦截整个对象，新增/删除属性、数组索引变化都能自动监听。
    
- 不需要递归遍历，性能更好。
    
- 支持 `Map`、`Set` 等更多数据结构。
---
#### 虚拟 DOM 是什么？有什么作用？

总结：
虚拟 DOM 是用 JavaScript 对象来描述真实 DOM 结构的一种技术。它的核心作用是减少直接操作真实 DOM 的次数。

数据变化时，框架先在内存中生成新的虚拟 DOM 树，然后通过算法对比新旧两棵树的差异，最后只把变化的部分一次性更新到真实 DOM 上，避免了频繁的重排和重绘。

虚拟 DOM 的优点是减少 DOM 操作、支持跨平台、提升开发效率。缺点是首次渲染会多一步创建虚拟 DOM 的开销，并且需要额外内存。但实际项目中，这些代价远小于它带来的可维护性和性能优化收益。

---
1. 什么是虚拟 DOM？

	**虚拟 DOM** 是一个轻量级的 JavaScript 对象，它描述了真实 DOM 的结构。你可以把它理解为真实 DOM 的一份“内存镜像”。

```js
// 真实 DOM
<div id="app" class="container">
  <p>Hello</p>
</div>

// 对应的虚拟 DOM（简化版）
{
  tag: 'div',
  props: { id: 'app', class: 'container' },
  children: [
    {
      tag: 'p',
      props: {},
      children: ['Hello']
    }
  ]
}
```

在 Vue 和 React 中，每个组件渲染时都会生成一棵虚拟 DOM 树。

---
2. 为什么需要虚拟 DOM？（解决了什么问题）

**直接操作真实 DOM 很昂贵。** 每次修改 DOM，浏览器都要重新计算样式、布局、绘制，频繁操作会造成性能瓶颈。

虚拟 DOM 的核心思路是：

1. **在内存中完成所有计算**：数据变化时，先生成一棵新的虚拟 DOM 树。
    
2. **Diff 对比新旧两棵树**：找出哪些地方真正变了。
    
3. **一次性最小化更新真实 DOM**：只把变化的部分应用到页面上，而不是整个页面重新渲染。

```
数据变化 → 生成新虚拟 DOM → Diff 对比 → 最小化更新真实 DOM
```
---
**好处**：

- **减少 DOM 操作次数**：把多次可能的 DOM 操作合并成一次最小的更新。
    
- **跨平台能力**：虚拟 DOM 本身是 JavaScript 对象，不依赖浏览器环境，因此可以渲染到不同平台（如浏览器 DOM、原生应用、小程序等）。
    
- **开发体验提升**：开发者只需要关注数据变化，不需要手动操作 DOM。
---
3. 工作流程

-  **首次渲染**：组件渲染生成虚拟 DOM，然后一次性创建真实 DOM 插入页面。
    
-  **数据更新**：组件重新渲染，生成新的虚拟 DOM 树。
    
-  **Diff 算法**：比较新旧两棵虚拟 DOM 树，找出差异（哪些节点变了、哪些没变）。
    
-  **Patch 更新**：只把变化的部分更新到真实 DOM，未变的部分保持不变。
---
4. 虚拟 DOM 的优缺点

|优点|说明|
|---|---|
|**减少 DOM 操作**|批量计算差异，一次性更新，避免频繁重排重绘|
|**跨平台**|JS 对象与平台无关，可渲染到 DOM、Native、小程序等|
|**开发效率高**|声明式开发，无需手动操作 DOM|

|缺点|说明|
|---|---|
|**首次渲染更慢**|需要先创建虚拟 DOM，再创建真实 DOM，比直接操作 DOM 多一步|
|**内存占用**|需要额外存储虚拟 DOM 树|
|**无法做到极致性能**|对于极其简单的静态页面，直接操作 DOM 可能更快|

但总体来说，虚拟 DOM 带来的开发效率提升和可维护性收益，远大于微小的性能损失。

---
### **组件：**

#### Vue 的组件通信方式有哪些？（核心4种）

1. Props / Emit（父子通信，最常用）

- **父传子**：父组件通过 `props` 把数据传给子组件。
- **子传父**：子组件通过 `$emit` 触发事件，父组件监听事件。

```vue
<!-- 父组件 -->
<Child :count="count" @increment="count++" />

<!-- 子组件 -->
<script setup>
defineProps({ count: Number })
const emit = defineEmits(['increment'])
</script>
<button @click="emit('increment')">+1</button>
```

**适用场景**：直接的父子关系，最规范、最常用的方式。

---
2. v-model（父子双向绑定）

	本质上是 `props` + `emit` 的语法糖，用于实现父子组件间的双向数据同步。

```vue
<!-- 父组件 -->
<Child v-model="message" />

<!-- 子组件 -->
<script setup>
const props = defineProps({ modelValue: String })
const emit = defineEmits(['update:modelValue'])
</script>
<input 
  :value="props.modelValue" 
  @input="emit('update:modelValue', $event.target.value)" 
/>
```

**适用场景**：表单组件、需要双向绑定的封装组件。Vue 3 支持多个 `v-model`(如 `v-model:title`)。

---
3. provide / inject（跨层级依赖注入）

	父组件用 `provide` 提供数据，任意深度的后代组件用 `inject` 获取，不需要逐层传递 props

```vue
<!-- 祖先组件 -->
<script setup>
import { provide, ref } from 'vue'
const theme = ref('dark')
provide('theme', theme)
</script>

<!-- 后代组件（任意深度） -->
<script setup>
import { inject } from 'vue'
const theme = inject('theme')
</script>
```

**适用场景**：主题、语言、用户信息等需要跨多层传递的全局配置。

**注意**：默认不是响应式的，需要传 `ref` 或 `reactive` 对象才能保持响应式。

---
4. ref（父组件访问子组件实例）

	父组件通过 `ref` 获取子组件实例，直接调用子组件暴露的方法或属性。

```vue
<!-- 父组件 -->
<Child ref="childRef" />
<button @click="childRef.focus()">聚焦</button>

<script setup>
const childRef = ref(null)
</script>

<!-- 子组件 -->
<script setup>
function focus() { /* ... */ }
defineExpose({ focus })  // <script setup> 必须显式暴露
</script>
```

**适用场景**：需要命令式调用子组件方法（如聚焦、重置表单、触发动画）。

**注意**：应该作为最后手段，优先使用 `props` 和 `emit`。

---
其他：
- Pinia
	**适用场景**：用户信息、购物车、多组件共享的复杂状态。

- 第三方库 `mitt` ：
	Vue 3 移除了 `$on`/`$emit` 全局事件总线，推荐用第三方库 `mitt` 实现兄弟组件通信。
	
	**适用场景**：没有直接关系的组件之间通信（兄弟组件、跨组件）。
	
	**注意**：不适合大型项目，事件流向难以追踪，优先用 Pinia。

- `$attrs` / `$listeners`（属性透传）
	父组件传递的非 `props` 属性会自动透传给子组件的根元素，或通过 `v-bind="$attrs"` 传递给深层组件。
	
	**适用场景**：封装高阶组件、透传原生属性。

- 路由参数 / query（页面间通信）
	通过 Vue Router 的路由参数或查询参数在页面之间传递数据。
	
	**适用场景**：页面级别的跳转传参。

---
📊 总结表格

|通信方式|方向|适用场景|重要程度|
|---|---|---|---|
|**props / emit**|父 ↔ 子|直接父子通信|🥇 必须|
|**v-model**|父 ↔ 子|双向绑定|🥇 必须|
|**provide / inject**|祖先 → 后代|跨层级注入|🥇 必须|
|**ref + defineExpose**|父 → 子|命令式调用|🥈 常用|
|**Pinia / Vuex**|全局|全局状态共享|🥈 常用|
|**mitt 事件总线**|任意组件|兄弟/跨组件|🥉 了解|
|**$attrs 透传**|父 → 子|属性透传|🥉 了解|
|**路由参数**|页面间|页面跳转传参|🥉 了解|

---
#### Props 和 Emits 如何使用？

父组件通过`props`向子组件传递数据，子组件通过`$emit`和父组件通信。

子组件使用 `defineProps` 声明接收的属性：
```vue
<!-- Child.vue -->
<script setup>
// 方式一：数组语法（仅声明名称）
const props = defineProps(['title', 'count'])

// 方式二：对象语法（带类型、默认值、校验）
const props = defineProps({
  title: { type: String, required: true },
  count: { type: Number, default: 0 },
  status: {
    type: String,
    default: 'active',
    validator: (value) => ['active', 'inactive'].includes(value)
  }
})

// 方式三：TypeScript 类型声明（推荐在 TS 项目中）
interface Props {
  title: string
  count?: number
}
const props = withDefaults(defineProps<Props>(), {
  count: 0
})
</script>

<template>
  <h2>{{ props.title }}</h2>
  <p>计数：{{ props.count }}</p>
</template>
```
父组件使用 `v-bind` 或简写 `:` 传递：
```vue
<!-- Parent.vue -->
<Child title="你好" :count="10" />
```

子组件使用 `defineEmits` 声明可触发的事件：
```vue
<!-- Child.vue -->
<script setup>
// 方式一：数组语法（仅声明事件名）
const emit = defineEmits(['update', 'delete'])

// 方式二：对象语法（带参数验证）
const emit = defineEmits({
  update: (value) => typeof value === 'string',
  delete: (id) => Number.isInteger(id)
})

// 方式三：TypeScript 类型声明
const emit = defineEmits<{
  (e: 'update', value: string): void
  (e: 'delete', id: number): void
}>()

function handleClick() {
  emit('update', '新数据')    // 触发事件并传递参数
}
</script>
```
父组件使用 `v-on` 或简写 `@` 监听：
```vue
<!-- Parent.vue -->
<Child @update="handleUpdate" @delete="handleDelete" />

<script setup>
function handleUpdate(value) {
  console.log('更新：', value)
}
function handleDelete(id) {
  console.log('删除：', id)
}
</script>
```

---
#### 插槽（Slot）是什么？有哪些类型？

插槽（Slot）是 Vue 中一种**内容分发机制**，允许父组件向子组件注入模板片段，实现组件的**可复用性和灵活性**。它解决了“组件结构固定但内容可变”的问题。

1. 默认插槽（单个）
	子组件中只有一个 `<slot>`，父组件中传入的所有内容都会渲染到这个位置。
```vue
<!-- 子组件 Card.vue -->
<template>
  <div class="card">
    <slot>默认内容</slot>
  </div>
</template>

<!-- 父组件使用 -->
<Card>
  <p>这是自定义内容</p>
</Card>
```

2. 具名插槽（多个）
	子组件中有多个 `<slot>` 需要区分，使用 `name` 属性命名。父组件使用 `<template v-slot:名字>` 或简写 `#名字` 来指定内容插入哪个插槽。
```vue
<!-- 子组件 Layout.vue -->
<template>
  <div>
    <header><slot name="header" /></header>
    <main><slot /></main> <!-- 默认插槽，name 为 default -->
    <footer><slot name="footer" /></footer>
  </div>
</template>

<!-- 父组件 -->
<Layout>
  <template #header>
    <h1>标题</h1>
  </template>
  <p>主体内容</p>
  <template #footer>
    <p>页脚</p>
  </template>
</Layout>
```

3. 作用域插槽（数据从子传父）
	子组件在插槽上通过 `v-bind` 暴露数据，父组件通过 `v-slot` 接收并使用这些数据。这是插槽中最强大的类型，实现了**数据子传父**的能力。
```vue
<!-- 子组件 -->
<slot :user="user" :age="age"></slot>

<!-- 父组件 -->
<Child v-slot="{ user, age }">
  <p>{{ user.name }} - {{ age }}</p>
</Child>
```
在具名插槽中使用作用域数据：
```vue
<Child>
  <template #header="headerProps">
    <h1>{{ headerProps.title }}</h1>
  </template>
</Child>
```

4. 动态插槽名（进阶）
	`v-slot` 支持动态参数，可以根据变量决定插槽名。
```vue
<template #[dynamicSlotName]>
  <p>动态内容</p>
</template>
```

---
插槽的作用：

- **内容复用**：组件只管结构和逻辑，内容由使用方决定。
- **布局抽象**：把通用布局（如卡片、弹窗、布局）封装成组件，不同页面填充不同内容。
- **数据传递**：作用域插槽让子组件能够把数据交给父组件渲染，实现了逻辑与展示的分离。
- **提高可维护性**：减少重复代码，修改布局只需修改组件本身。

---
#### keep-alive 有什么作用？

`<KeepAlive>` 是一个内置组件，它的功能是在多个组件间动态切换时缓存被移除的组件实例。用于**缓存不活动的组件实例**，避免组件在切换时被销毁和重新创建。它的核心作用是**保留组件状态、减少重复渲染、提升性能**。

---
基本用法：
```vue
<template>
  <button @click="show = !show">切换</button>
  <keep-alive>
    <component :is="show ? 'CompA' : 'CompB'" />
  </keep-alive>
</template>

<script setup>
import { ref } from 'vue'
import CompA from './CompA.vue'
import CompB from './CompB.vue'
const show = ref(true)
</script>
```
---
常用配置：

|属性|类型|作用|
|---|---|---|
|`include`|字符串 / 正则 / 数组|只缓存匹配的组件（按组件 `name` 匹配）|
|`exclude`|字符串 / 正则 / 数组|排除匹配的组件，不缓存|
|`max`|数字|最多缓存多少个组件实例，超过时销毁最久未使用的（LRU）|
```vue
<!-- 以英文逗号分隔的字符串 -->
<KeepAlive include="a,b">
  <component :is="view" />
</KeepAlive>

<!-- 正则表达式 (需使用 `v-bind`) -->
<KeepAlive :include="/a|b/">
  <component :is="view" />
</KeepAlive>

<!-- 数组 (需使用 `v-bind`) -->
<KeepAlive :include="['a', 'b']">
  <component :is="view" />
</KeepAlive>
```

它会根据组件的 [`name`](https://cn.vuejs.org/api/options-misc.html#name) 选项进行匹配，所以组件如果想要条件性地被 `KeepAlive` 缓存，就必须显式声明一个 `name` 选项。

---
生命周期钩子：

被 `<keep-alive>` 包裹的组件会额外获得两个生命周期钩子：
- **`activated`**：组件被缓存后重新激活时调用。
- **`deactivated`**：组件被缓存时调用。
```vue
<script setup>
import { onActivated, onDeactivated } from 'vue'

onActivated(() => {
  // 调用时机为首次挂载
  // 以及每次从缓存中被重新插入时
})

onDeactivated(() => {
  // 在从 DOM 上移除、进入缓存
  // 以及组件卸载时调用
})
</script>
```

---
与 Router 结合（最常见场景）：

在 Vue Router 中，`<keep-alive>` 通常包裹 `<router-view>` 来实现页面级缓存：
```vue
<template>
  <router-view v-slot="{ Component }">
    <keep-alive include="UserList">
      <component :is="Component" />
    </keep-alive>
  </router-view>
</template>
```
这样，当用户从列表页进入详情页再返回时，列表页的滚动位置、筛选条件等状态得以保留。

---
注意点：

- 被缓存的组件不会触发 `mounted` / `unmounted`，而是 `activated` / `deactivated`。
- 不要滥用缓存，否则可能导致内存占用过高。通过 `max` 限制缓存数量，或用 `include`/`exclude` 精确控制。
- 缓存组件的 `data` 状态始终保存在内存中，可能不是所有组件都适合缓存（如包含定时器的组件需要在 `deactivated` 中清理）。
---
### **Composition API：**

1. Composition API 和 Options API 有什么区别？
2. ref 和 reactive 有什么区别？
3. computed 和 watch 有什么区别？
4. setup 函数有什么作用？
---
### **Vue Router：**

1. Vue Router 的路由模式有哪些？
2. 路由守卫有哪些？如何使用？
3. 如何实现路由懒加载？
---
### **Pinia：**

1. Pinia 和 Vuex 有什么区别？
2. Pinia 如何使用？
---
# react
## 经典面试题
### **基础概念：**

1. React 有什么特点？
2. 什么是虚拟 DOM？有什么作用？
3. 什么是 JSX？
4. React 的单向数据流是什么？

### **组件：**

1. 函数组件和类组件有什么区别？
2. Props 是什么？如何使用？
3. 受控组件和非受控组件有什么区别？

### **Hooks：**

1. 什么是 Hooks？为什么要使用 Hooks？
2. useState 和 useEffect 如何使用？
3. useEffect 的依赖数组有什么作用？
4. useMemo 和 useCallback 有什么区别？
5. 如何自定义 Hooks？

### **性能优化：**

1. React 如何进行性能优化？
2. 什么是 React.memo？
3. 虚拟 DOM 的 Diff 算法是怎样的？

### **状态管理：**

1. Context API 如何使用？
2. Redux 的工作原理是什么？
---
# 计算机网络
## 经典面试题

1. 计算机网络分层结构，各层有哪些常用协议？【高频】
2. TCP 和 UDP 协议的区别？如何选择应用哪个协议？【高频】
3. TCP 为什么需要三次握手和四次挥手？为什么不是两次握手、四次握手？为什么不是三次挥手？【高频】
4. TCP 滑动窗口和拥塞控制机制
5. TCP 如何保证可靠性？
6. HTTP 协议常用状态码和含义
7. HTTP 和 HTTPS 协议的区别？【高频】
8. HTTPS 的原理、请求流程？
9. HTTP 2 为什么快？
10. HTTP 的短连接和长连接，以及如何选择？
11. POST 和 GET 请求的区别【高频】
12. 浏览器中输入URL 返回页面的过程？（包括 DNS 解析过程）【高频】
13. Cookie 和 Session 的区别？
---
# 链接知识

## **函数柯里化**①

**柯里化**：把一个需要**多个参数**的函数，变成一个**每次只接收一个参数、返回一个新函数**的链式调用方式。每次调用都通过闭包记住已经传入的参数。

```js
// 普通函数：三个参数一起传
function add(a, b, c) {
  return a + b + c
}
add(1, 2, 3) // 6

// 柯里化版本：每次只传一个参数
function curryAdd(a) {
  return function(b) {
    return function(c) {
      return a + b + c
    }
  }
}
curryAdd(1)(2)(3) // 6
```

**原理**：每次返回的新函数，都是一个闭包，记住了之前传进来的参数。最后所有参数凑齐了，才真正执行计算。

**实际价值**：

- **参数复用**：比如封装一个 `double = curryMultiply(2)`，之后调用 `double(5)` 就行。
    
- **延迟执行**：把参数分步传入，最后一步才真正执行。
    
- **函数组合**：在函数式编程中，柯里化让函数更容易组合和复用。
---
## Generator 函数和`yield`②
简单说，**Generator 是一个可以中途暂停并返回多次值的函数**，`yield` 是它用来“暂停并输出值”的关键字。它曾是异步流程控制的基石，但现代开发中已被 `async/await` 取代
```js
// 注意 function 后面的 * 号
function* myGenerator() {
  yield '你好';  // 第一次暂停，返回 '你好'
  yield '世界';  // 第二次暂停，返回 '世界'
  return '结束';  // 第三次，函数结束，返回 '结束'
}

const gen = myGenerator(); // 调用后，函数体不会执行，返回一个迭代器

console.log(gen.next()); // { value: '你好', done: false }
console.log(gen.next()); // { value: '世界', done: false }
console.log(gen.next()); // { value: '结束', done: true }
console.log(gen.next()); // { value: undefined, done: true }
```
---
